// Prediction markets are disabled for now: predictions are cut from consumer
// v1 (no backend auth or settlement yet), so the operations surfaces hide too.
// Flipping this restores the Markets nav section, the home "Markets due" tile,
// and market search results. Market data and MarketsPage stay intact.
export const PREDICTION_MARKETS_ENABLED = false;
